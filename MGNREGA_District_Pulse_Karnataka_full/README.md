# MGNREGA District Pulse — Karnataka (Full Starter)

This repository contains a full starter pack for the Build for Bharat Fellowship assignment:
- Kannada (neutral) UI (React PWA)
- Backend (Spring Boot skeleton + controllers)
- ETL (Python) to ingest MGNREGA Open API data
- Postgres + PostGIS schema + district GeoJSON loader
- Docker Compose for local/EC2 deployment
- Nginx reverse proxy config and systemd unit example for ETL scheduling

**Important:** Replace placeholders (API resource id, API key) and provide Karnataka district GeoJSON.
See docs/deploy.md for EC2 deployment steps.
